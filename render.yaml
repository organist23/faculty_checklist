services:
  - type: static
    name: faculty-checklist-app
    env: static
    buildCommand: npm run build
    publishDir: dist
    routes:
      - type: rewrite
        source: /([^.]*)
        destination: /index.html
